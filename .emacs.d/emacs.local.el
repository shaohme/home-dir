;;; package --- local init file
;;; Commentary:
;;; Code:

(setq default-frame-alist '((font . "monospace 14")))

;;(use-package tramp
;;  :ensure t
;;  :config
;;  (add-to-list 'tramp-default-method-alist
;;               '("anon.gotu.dk" "root" "ssh"))
;;  )



(provide 'emacs.local)
;;; emacs.local.el ends here
